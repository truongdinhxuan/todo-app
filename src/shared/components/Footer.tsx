import styles from "../style/styleLayout.module.css"
const Footer = () => {
    return (
        <section className={styles.footer}>
            <h1>Footer</h1>
        </section>
    )
}

export default Footer;